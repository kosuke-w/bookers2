<main>
  <div>
    <div>
      <div>
        <h2>User info</h2>
        user画像
        <table>
          <tbody>
            <tr></tr>
            <tr>
              <th>name</th>
              <th></th>
            </tr>
            <tr>
              <th>introduction</th>
              <th></th>
            </tr>
          </tbody>
        </table>
        <div>bottun</div>

        <h2>New book</h2>
        <%= form_with model: @book_new, url:'/books', local:true do |f| %>
          <h4>Title</h4>
          <%= f.text_field :title %>
          <h4>Opinion</h4>
          <%= f.text_area :body %>
          <%= f.submit 'Create Book' %>
        <% end %>
      </div>

      <div>
        <h2>Books</h2>
        <table>
          <thead>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
          </thead>
          <tbody>
            <% @books.each do |book| %>
              <tr>
                <td>user画像</td>
                <td>
                  <%= link_to book_path(book.id) do %>
                    <%= book.title %>
                  <% end %>
                </td>
                <td><%= book.body %></td>
              </tr>
            <% end %>
            <%= paginate @books %>
          </tbody>
        </table>
      </div>

    </div>
  </div>

</main>
