<main>
  <div>
    <div>
      <div>
        <%= render 'books/info', book: @book %>
      </div>

      <div>
        <h2>Books</h2>
        <table>
          <thead>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
          </thead>
          <tbody>
            <% @books.each do |book| %>
              <tr>
                <td>user画像</td>
                <td>
                  <%= link_to book_path(book.id) do %>
                    <%= book.title %>
                  <% end %>
                </td>
                <td><%= book.body %></td>
              </tr>
            <% end %>
            <%= paginate @books %>
          </tbody>
        </table>
      </div>

    </div>
  </div>

</main>
