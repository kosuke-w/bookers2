<!DOCTYPE html>
<html>
  <head>
    <title>bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/809f7a69fb.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbolinks-track": "reload" %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg nabvar-dark bg-dark">
        <div class="container">
          <%= link_to "Bookers", root_path, class: "navbar-brand text-light" %>
          <div class="row">
          <% if user_signed_in? %>
            <ul class="navbar-nav ml-auto">
              <li><%= link_to " Home", "/users/#{current_user.id}", class: "fas fa-home nav-text nav-link text-light"  %></li>
              <li><%= link_to " Users", users_path, class: "fas fa-users nav-text nav-link text-light" %></li>
              <li><%= link_to " Books", books_path, class: "fas fa-book-open nav-text nav-link text-light" %></li>
              <li><%= link_to " logout", destroy_user_session_path, method: :delete, class: "fas fa-sign-out-alt nav-text nav-link text-light" %></li>
            </ul>
          <% else %>
            <ul>
              <li><%= link_to " Home", root_path, class: "fas fa-home nav-text nav-link text-light" %></li>
              <li><%= link_to " About", home_about_path, class: "fas fa-link nav-text nav-link text-light" %></li>
              <li><%= link_to " sign up", new_user_registration_path, class: "fas fa-user-plus nav-text nav-link text-light" %></li>
              <li><%= link_to " login", new_user_session_path, class: "fas fa-sign-in-alt nav-text nav-link text-light" %></li>
            </ul>
          <% end %>
          </div>
        </div>
      </nav>
    </header>

    <main>
      <% flash.each do |message_type, message| %>
        <%= content_tag :div, message, class: "message_type" %>
      <% end %>

      <%= yield %>
    </main>

    <footer>
      <div class="container mt-5">
        <div class="row">
          <div class="mx-auto">
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>